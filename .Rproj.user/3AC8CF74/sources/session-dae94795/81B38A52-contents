---
title: "R программын танилцуулга"
author: "Я.Гансүх"
date: "2024-11-01"
output: 
  html_document:
    toc: true
    toc_depth: 4
    toc_float: true
    number_sections: true
editor_options: 
  markdown: 
    wrap: sentence
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

### **R болон RStudio-г танин мэдэх, ашиглах**

R программ суулгах: <http://www.r-project.org/>.

RStudio суулгах: <http://www.rstudio.com/>.

#### **R гэж юу вэ?**

-   R бол үнэгүй нээлттэй эхийн програмчлалын хэл, орчин юм.
-   Энэ нь өгөгдлийн шинжилгээ, график, өгөгдлийн симуляцид зориулагдсан.
-   Энэ нь статистик, програмчлалын дэлхийн тэргүүлэх программ хангамж, цогц систем юм.

#### **Яагаад R ашиглах ёстой вэ?**

-   R бол үнэ төлбөргүй, нээлттэй эх сурвалж: танд болон хүн бүрт зориулагдсан;
-   R нь түгээмэл хэрэглээтэй: өргөн хүрээтэй хэрэглэгчийн бааз нь статистикийн хэрэгслийг үргэлжлүүлэн хөгжүүлэх, хадгалахад тусалдаг;
-   R нь хүчирхэг: Та нарийн төвөгтэй симуляцийг програмчилж болно.
-   R бүх өргөтгөлүүдийг дэмждэг.
-   R нь ихэнх үйлдлийн системүүд дээр ажилладаг, бусад хэлтэй холбогддог: C++, Java, Python, Julia, Stan болон бусад!

Нэмэлт программ, ажиллагаа шаардахгүйгээр шууд үр дүн, тайлан хийж болно.

```{r echo=FALSE, fig.height=18, fig.asp=0.7, fig.align = 'center', fig.cap='**_Зураг 1. R программ ашигласнаар үүсэх давуу тал_**'}
knitr::include_graphics(rep("images/01.png", 1))
```

Жил бүр илүү олон эрдэмтэд ашигласаар байна!
2025 оны 2-р сарын байдлаар Comprehensive R Archive Network (CRAN)-д 22000 гаруй багц (мөн Github repositories дотор олон мянган багц) бүртгэгдсэн байна!

```{r echo=FALSE, out.width="50%", fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 2. R программ ашигласнаар үүсэх давуу тал_**'}
knitr::include_graphics(rep("images/02.png", 1))
```

Төрөл бүрийн дүн шинжилгээнүүдийг өөр өөр төрлөөр нэг талбар дээр харуулах бүрэн боломжтой байдаг.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 3. Эдгээр бүх графикыг R ᠋программ дээр хийсэн._**' , out.width = "70%"}
knitr::include_graphics(rep("images/03.png", 1))
```

#### **RStudio гэж юу вэ?**

-   RStudio нь R-д зориулсан хамгийн их ашиглагддаг функц, үйлдлүүдийг нэгтгэн хөгжлүүлсэн талбар (IDE) юм.
-   Энэ нь график, ажлын түүх, алдааг олох, ажлын талбарыг удирдах хэрэгсэл, шууд кодын гүйцэтгэлийг дэмждэг консол, синтакс тодруулагч хянагчийг агуулдаг.
-   Энэ нь R (болон бусад програмчлалын хэлүүд) -тэй нэгтгэгдэж, олон ашигтай функцийг хангадаг.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 4. RStudio программыг суулгасны дараа нээхэд дээрх байдалтай харагдана_**' , out.width = "70%"}
knitr::include_graphics(rep("images/04.png", 1))
```

-   Та RStudio-г анх удаа нээхэд дэлгэц нь гурван үндсэн самбар бүлэгт хуваагдана:
    -   Console, Terminal, Job group;
    -   Environment, History, Connections group;
    -   Files, Plot, Packages, Help, Viewer panes; and, Script pane group.
-   Скрипт нээх эсвэл шинэ скрипт үүсгэсний дараа (File \> New File \> R Script or Ctrl/Cmd + Shift + N) дөрөв дэх самбар гарч ирнэ!

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 5. RStudio программын үндсэн самбарууд_**' , out.width = "70%"}
knitr::include_graphics(rep("images/05.png", 1))
```

-   Ихэвчлэн console хэсэгт харагдах эхний текст нь RStudio-ийн ашиглаж байгаа R хувилбар юм.
-   Console бол R таныг юу хийхийг хэлэхийг хүлээж байгаа газар бөгөөд тантай харилцах бөгөөд таны кодын тушаалын үр дүнг харуулдаг.
-   R командыг хүлээн авахад бэлэн болсон үед **\>** prompt харуулах болно.

### **R-г тооны машин шиг ашиглая!**

#### **Арифметик үйлдлүүд**

Нэмэх, хасах үйлдлүүдийг ашиглан үр дүн шууд гарган авах боломжтой.

```{r }
1 + 1
```

```{r }
10 - 1
```

Үржих, хуваа үйлдлүүд мөн адил байна.

```{r }
2 * 2
```

```{r }
8 / 2
```

Зэрэгт дэвшүүлэхдээ **\^** тэмдэгтийг ашиглана.

```{r }
2^3
```

```{r }
3^4
```

Эдгээр үйлдлүүдийг нэгэн зэрэг ашиглах боломжтой бөгөөд бодолтын дараалал нь R эх хэлэнд програмчлагдсан байдаг.

```{r }
2 + 16 * 24 - 56 / (2 + 1) - 457
```

Энгийн бодолт хийж үзье: Тойргийн радиус 5 см бол талбайг олно уу?
Бодолтын томьёо нь $$Area = pi * r^2$$ байх бөгөөд R программын консол дээр дараах байдлаар бичнэ.

```{r }
3.1416 * 5^2
```

Мөн математик тооцооны тогтмол утгууд R эх хэлэнд програмчлагдсан байдаг.
Иймд шууд **pi** гэж бичиж болно.

```{r }
pi * 5^2
```

### **Объектуудыг R дээр зохион байгуулах!**

#### **R нь объектод суурилсан орчин юм.**

Та **\<-** хуваарилах операторыг ашиглан нэрлэсэн объектуудад мэдээлэл өгч болно.

```{r }
Four_letters <- "ACDC"
Four_letters
```

Өөрөөр хэлбэл, "ACDC" объектод Four_letters утга оноож (оноосон нэр) өгснөөр Four_letters-ийг дуудахад "ACDC"-аар хариу өгдөг.

```{r }
my_birthday_month <- 9
my_birthday_month
```

```{r }
Бодолтын_хариу <- 2 + 16 * 24 - 56 / (2 + 1) - 457
Бодолтын_хариу
```

**Анхаараарай!** Бага (\<) болон хасах (-) тэмдгүүдийн хооронд зай авахгүй, шууд бичигдсэн **\<-** байх хэрэгтэй.

#### **Объектыг нэрлэх дүрэм**

Объектуудын нэрэнд зөвхөн дараах зүйлс багтаж болно:

-   Үсэг: a-z, A-Z (Монгол хэлний крилл үсгийг ашиглаж болох ч зарим тохиодолд алдаа заадаг.)
-   Тоо: 0-9
-   Тэмдэгт: цэг (**.**) болон доогуур зураас (\_) ашиглаж болно.

Цаашид анхаарах энгийн дүрмүүд:

-   Объектуудын нэр ямагт үсгээр эхлэх ёстой. **Forest_data001** эсвэл **F_data_001**
-   R дээр том, жижиг үсгүүд ялгаатай. **Data_1** нь **data_1**-ээс өөр.
-   Тусгай тэмдэгт **(\@, /, \# гэх мэт)** ашиглах боломжгүй.
-   Объектын нэр өвөрмөц, дахин давтагдахгүй байх ёстой. **data_1 \<- 1** нь өмнө нь үүсгэсэн **data_1** нэртэй объектуудыг дарж шинэ объект үүсгэдэг.

```{r }
Forest_data001 <- 2025
Forest_data001
F_data_001 <- 2024
F_data_001
Data_1 <- "Том үсгээр эхэлсэн 1-р өгөгдөл"
Data_1
data_1 <- "жижиг үсгээр эхэлсэн 1-р өгөгдөл"
data_1
```

Объектыг нэрлэх, код бичих анхаарах зүйлс:

-   Богино, тодорхой нэр илүү тохиромжтой байдаг.
-   Хувьсагчийг **var** гэж нэрлэх нь төөрөгдөл үүсгэнэ.
-   Доогуур зураас ( \_ ) эсвэл цэг ( . ) ашиглан нэр доторх үгсийг салгаж болно. **avg_richness** эсвэл **avg.richness** нь **avgrichness**-тэй харьцуулахад илүү хялбар уншигддаг
-   Одоо байгаа функц эсвэл тогтмолуудын нэрийг (жишээ нь, **c, хүснэгт, T, матриц**) ашиглахаас зайлсхий.
-   Кодыг уншихад хялбар болгохын тулд операторуудын **(=, +, -, \<- гэх мэт)** эргэн тойронд зай **(space)** авна уу.
-   Таслалын дараа үргэлж зай **(space)** авна уу.

### **Өгөгдлийн төрөл болон бүтэц**

#### **Өгөгдлийн төрөл (data types)**

Өгөгдлийн төрлүүд нь R-дээрх утгууд, өгөгдлүүд хэрхэн хадгалагдахыг тодорхойлдог.

Бид typeof() функцыг ашиглан объектын төрөл, горимыг тодорхойлох боломжтой.
Өгөгдлийн үндсэн төрлүүд нь:

-   Тоон өгөгдөл - Numeric (integer-бүхэл, double-бутархай)
-   Тэмдэгт өгөгдөл – Character (заавал “ ” хаалтанд байна)
-   Логик өгөгдөл – Logical (TRUE or FALSE)

#### **Өгөгдлийн бүтэц (data structures)**

Тоо эсвэл текстийн мөр зэрэг нэг төрлийн объектыг **скаляр (scalar)** бүтэцтэй өгөгдөл гэж нэрлэдэг.

```{r }
a <- 100
a # 
b <- 3 / 100
b
c <- (a +b) / b
c
```

**a,b,c** өгөгдөлүүд нь ямар нэг (ганцхан ширхэг) тоо агуулсан учир скаляр (scalar) бүтэц болно.

```{r }
d <- "species"
d # 
e <- "тархалт"
e
f <- "When is the next pause again?"
f
```

**d,e,f** өгөгдөлүүд нь ямар нэг тэмдэгт ("" дотор учраас ганцхан тэмдэгт гэж үзнэ) агуулсан учир скаляр (scalar) бүтэц болно.

**Вектор (vector)** бүтэцтэй объект нь нэг объект хэлбэрээр хадгалагдсан хэд хэдэн **скаляруудын нэгдэл** юм.
Скаляруудын нэгэн адил векторууд нь тоон, логик, тэмдэгтийн төрлүүд байж болох ч тэдгээрийг хэзээ ч хамтад нь бичиж болохгүй!

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 6. Скаляр болон вектор өгөгдлийн дүрслэл_**' , out.width = "30%"}
knitr::include_graphics(rep("images/06.png", 1))
```

R дээр вектор бүтэцтэй өгөгдөл үүсгэх олон арга бий.
Жишээлбэл:

```{r echo= FALSE}
my_table <- data.frame(
  Function = c("c(a, b, ...)", "a:b", "seq(from, to, by, length.out)", "rep(x, times, each, length.out)"),
  Example = c("c(1, 3, 5, 7, 9)", "1:5", "seq(from = 0, to = 6, by = 2)", 'rep(c(7, 8), times = 2, each = 2)'),
  Result = c("1, 3, 5, 7, 9", "1, 2, 3, 4, 5",  "0, 2, 4, 6", "7, 7, 8, 8, 7, 7, 8, 8"))

knitr::kable(
  my_table, booktabs = TRUE, longtable = TRUE, align = "ccc",
  caption = 'Хүснэгт 1. Зарим функцийн бичиглэлийн хэлбэр, жишээнүүд'
)
```

**c()** функц (combine нь нэгтгэх гэсэн үг бөгөөд тэдгээрийг нэгтгэх гэсэн утгатай) хэд хэдэн скалярыг аргумент болгон нэгтгэж, таслалаар тусгаарлаж, тэдгээрийг агуулсан векторыг үүсгэнэ.

```{r }
vector <- c("value1", "value2", "value3")
vector
class(vector)
```

**a:b** нь аргумент болгон хоёр тоон скаляр бөгөөд эхний a тооноос b тоо хүртэлх 1 нэгжийн алхамтай тооны векторыг үүсгэнэ.

```{r }
1:8
7.5:1.5
```

**seq()** функц нь **a:b** адил тоон дараалал үүсгэх бөгөөд нэмэлтээр алхам буюу интервалыг (**by** аргументаар), мөн цувааны уртыг (**lenth.out** аргументаар) тус тус тодорхойлдог.

```{r }
seq(from = 1, to = 10, by = 2)
seq(from = 20, to = 2, by = -2)
```

**rep()** нь скаляр (эсвэл вектор)-ыг тодорхой тооны удаа эсвэл хүссэн урттай давтах боломжийг олгодог:

```{r }
rep(x = 1:3, each = 2, times = 2)
rep(x = c(1, 2), each = 3)
```

Өгөгдлийн төрөл дээр суурилан вектор бүтцийг үүсгэж болдог.
Жишээ нь:

```{r }
num_vector <- c(1, 4, 32, -76, -4)
num_vector
class(num_vector)
char_vector <- c("blue", "red", "green", "pink")
char_vector
class(char_vector)
bool_vector <- c(TRUE, TRUE, FALSE) # or c(T, T, F)
bool_vector
class(bool_vector)
```

**Матриц (matrix)** бүтэц

-   Скаляр нь нэг элемент, векторууд нь нэг төрлийн нэгээс олон скаляр агуулдаг.
-   Матрицууд нь зэрэгцээ орших олон векторуудын нийлбэр юм.
-   Векторууд нэг хэмжээстэй байхад матрицууд нь мөр, баганаар тодорхойлогддог хоёр хэмжээстэй байдаг.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 7. Скаляр, вектор болон матриц өгөгдлийн дүрслэл_**' , out.width = "40%"}
knitr::include_graphics(rep("images/07.png", 1))
```

Матриц өгөгдлийг үүсгэж сурцгаая!

```{r }
matrix(data = 1:10,
       nrow = 5,
       ncol = 2)

matrix(data = 1:10,
       nrow = 2,
       ncol = 5)
```

Мөн бид **cbind()** (column binding) болон **rbind()** (row binding) ашиглан олон векторыг нэгтгэж болно.

```{r }
nickname <- c("kat", "gab", "lo")
nickname
animal <- c("dog", "mouse", "cat")
animal
```

```{r }
cbind(nickname, animal) # босоо баганан байдлаар матриц өгөгдөл үүсгэх
rbind(nickname, animal) # хэвтээ мөр байдлаар матриц өгөгдөл үүсгэх
```

Векторуудын нэгэн адил матриц дээр математик үйлдлүүд маш сайн ажилладаг.
Матрицын зүй тогтлыг сайн ажиглаарай.

```{r }
(mat_1 <- matrix(data = 1:9,
                 nrow = 3,
                 ncol = 3))
```

```{r }
(mat_2 <- matrix(data = 9:1,
                 nrow = 3,
                 ncol = 3))
```

```{r }
mat_1 * mat_2
```

```{r }
(step_1 <- matrix(data = 1:6,
                  nrow = 2,
                  ncol = 3))
```

```{r }
(step_2 <- matrix(
  data = c("cheetah", 
           "tiger", 
           "ladybug",
           "deer",
           "monkey",
           "crocodile"),
  nrow = 2,
  ncol = 3))
```

```{r }
Merged_matrix <- cbind(c(step_1), c(step_2))
Merged_matrix
```

**Data.frame** бүтэц нь матрицаас ялгаатай нь өгөгдлийн хүрээ нь тоон, тэмдэгт, логик багана (эсвэл вектор) агуулж болно.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 7. Скаляр, вектор, матриц болон дата.фрэйм (data frame) өгөгдлийн дүрслэл_**' , out.width = "50%"}
knitr::include_graphics(rep("images/08.png", 1))

```

Бодит амьдрал, судалгааны ажил олон төрлийн холимог өгөгдөл ашигладаг бөгөөд цаашдын хичээл, алхамуудад нилээн их ашиглагдах болно.
Хөрсний судалгааны ажлын хэсэгхэн үр дүнг жишээ байдлаар харуулья:

-   site_id дээж авсан газрын дугаар;
-   soil_pH хөрсний уусмалын орчин;
-   num_sp төрөл зүйлийн тоо;
-   fertilised бордоо хэрэглэсэн эсэх;

```{r echo= FALSE}
soil_fertilisation_data <- data.frame(
  site_id = c("A1.01", "A1.02", "B1.01", "B1.02"),
  soil_pH = c(5.6, 7.3, 4.1, 6.0),
  num_sp = c(17, 23, 15, 7),
  fertilised = c("yes", "yes", "no", "no"))

knitr::kable(
  soil_fertilisation_data, booktabs = TRUE, longtable = FALSE, align = "ccc",
  caption = 'Хүснэгт 2. Хөрсний судалгааны ажлын үр дүнгээс'
)
```

```{r echo=TRUE}
site_id <- c("A1.01", "A1.02", "B1.01", "B1.02")
soil_pH <- c(5.6, 7.3, 4.1, 6.0)
num_sp <- c(17, 23, 15, 7)
fertilised <- c("yes", "yes", "no", "no")
soil_fertilisation_data <- data.frame(site_id, soil_pH, num_sp, fertilised)
```

```{r echo=TRUE}
soil_fertilisation_data
```

### **Индексжүүлэлт (indexing)**

Бид өөрсдийн өгөгдөл, мэдээллийг вектор, матриц эсвэл data.frame хэлбэрээр R-д хадгалдаг.
Зарим тохиолдолд тодорхой шалгуурын дагуу зөвхөн тухайн өгөгдлийг харах, ашиглах эсвэл янз бүрийн байдлаар хуваах, бүлэглэх хэрэгцээ шаардлага үүсэж байдаг.

Бид **хаалт [ ]** дотор харахыг хүссэн утгуудын байрлалыг зааж өгч болно.
Үүнийг ихэвчлэн индексжүүлэх эсвэл хуваалт гэж нэрлэдэг.
Индексжүүлэлтийн гол бичиглэл нь: дөрвөлжин хаалт [ ] ба [ , ].

Вектор доторх элементийг [ ] ашиглан тухайн элементийг дуудахын тулд [ ] хаалтанд тухайн элементийн байрлалын дугаарыг бичих хэрэгтэй.
Энэхүү байрлалын дугаар нь өгөгдлийн утгаас үл хамааран **,** хязгаарлагдсан элементүүдийг 1-ээс эхлэн бүхэл тоон дарааллаар дугаарласныг хэлнэ.

Жишээлбэл, 1-ээс 9 хүртлэх сондгой тооны **odd_n** нэртэй цуваа үүсгэе.
Энэхүү вектор өгөгдөл 5 тооноос (1, 3, 5, 7, 9) бүрдэх бөгөөд 1-ээс 5 хүртэл дугаарлагдсан буюу индексжсэн байдаг.
**odd_n[2]** коммандаар 2-р утгыг сонгож уншуулахад **3** гэсэн хариу гарна.

```{r echo=TRUE}
(odd_n <- seq(1, 9, by = 2))
odd_n[2]
```

Бид мөн вектор доторх олон байрлалын утгыг **c()** коммандаар уншуулж болно.

```{r echo=TRUE}
odd_n[c(2, 4)]
```

Мөн бид тодорхой байрлалд хамаарах утгыг векторын утгын өмнөх хасах (-) тэмдгийг ашиглан харуулахгүй байж болно.

```{r echo=TRUE}
odd_n[-c(1, 2)]
odd_n[-4]
```

num_vector гэсэн нэр бүхий вектор дээр дараах үйлдлийг хийж үзье:

-   4-р утгыг гарган авах;
-   1 ба 3-р утгыг гарган авах;
-   2 ба 4-өөс бусад бүх утгыг гарган авах;
-   6-аас 10-р утгыг гарган авах.

```{r echo=TRUE}
num_vector <- c(1, 4, 3, 98, 32, -76, -4)
num_vector[4]
num_vector[c(1, 3)]
num_vector[c(-2, -4)]
num_vector[6:10]
```

**Data.frame** болон **матриц** дээр мөр, баганын дугаарыг ашиглан элементийг дуудах боломжтой.
Үндсэн зарчим тухайн аливаа нэг утга нь мөр, баганын уулзвар дээр байна.
data_frame_name[row_number, column_number] бүтэцтэй өгөгдлийн хувьд:

-   my_df[1, ] (1-р мөрийг бүхлээр нь дуудах)
-   my_df[, 3] (3-р баганыг бүхлээр нь дуудах)
-   my_df[2, 4] (2-р мөрний 4-р баганад харгалзах утгыг дуудах)

Эдгээр нь та бидний хүүхэд ахуй цагтаа тоглодог байсан **онгоц буудах** тоглоомын дүрэмтэй яг адилхан юм.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 9. Онгоц буудах тоглоомын үед  мөр, баганын дугаар ашиглан хэлдэг_**' , out.width = "30%"}
knitr::include_graphics(rep("images/09.png", 1))
```

Бид бүхэн өмнө нь **soil_fertilisation_data** нэртэй өгөгдөл үүсгэсэн, эндээс зөвхөн судалгааны байршлын дугаарыг, уусмалын орчны дүнтэй харах тохиолдолд:

```{r echo=TRUE}
soil_fertilisation_data
soil_fertilisation_data[ , c("site_id", "soil_pH")]
```

Өгөгдөл салган харах өөр нэг арга нь **\$** тэмдэгт ашиглах юм.

```{r echo=TRUE}
soil_fertilisation_data$soil_pH
```

Бид аливаа нэг нөхцлөөр ҮНЭН эсвэл ХУДАЛ утгыг авахын тулд төрөл бүрийн операторуудыг ашиглаж болно.
Доорх жишээнүүдийг харна уу: **odd_n** нь 1, 3, 5, 7, 9-г агуулж байгаа болно.

```{r echo= FALSE}
df <- data.frame(
  Operator = c("< and >", "<= and >=", "==", "!=", "x | y", "x & y", "x %in% y"),
  Description = c(
    "less than or greater than",
    "less/greater or equal to",
    "exactly equal to",
    "not equal to",
    "x OR y",
    "x AND y",
    "x match y"
  ),
  Example = c(
    "odd_n > 3",
    "odd_n >= 3",
    "odd_n == 3",
    "odd_n != 3",
    "odd_n[odd_n >= 5 | odd_n < 3]",
    "odd_n[odd_n >=3 & odd_n < 7]",
    "odd_n[odd_n %in% c(3,7)]"
  ),
  Result = c(
    "FALSE, FALSE, TRUE, TRUE, TRUE",
    "FALSE, TRUE, TRUE, TRUE, TRUE",
    "FALSE, TRUE, FALSE, FALSE, FALSE",
    "TRUE, FALSE, TRUE, TRUE, TRUE",
    "1, 5, 7, 9",
    "3, 5",
    "3, 7"
  ),
  stringsAsFactors = FALSE
)

knitr::kable(
  df, booktabs = TRUE, longtable = FALSE, align = "ccc",
  caption = 'Хүснэгт 3. Логик өгөгдлийг индексийн утга бүрд тодорхой нөхцөлтэйгээр ашиглах нь'
)
```

Өгөгдлийн бүтцүүдийг харьцуулан харах нь:

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 10. Өгөгдлийн бүтцүүдийн ялгааг товчоор илэрхийлбэл_**' , out.width = "60%"}
knitr::include_graphics(rep("images/10.png", 1))
```

### **Функц (function)**

Функц нь математикийн алхам бүрийг бичих шаардлагагүйгээр объектууд дээр үйлдлийг хурдан гүйцэтгэх боломжийг олгодог.
Өөрөөр хэлбэл, функц нь бидний амьдралыг хялбарчлах ёстой хэрэгсэл юм!
Функцид аргумент (эсвэл параметр) буюу утгууд оруулах хэрэгтэй.
Дараа нь эдгээр аргументуудыг ашиглан (функц ажиллах бөгөөд) үйлдлүүдийг хийж, гаралтыг үзүүлдэг.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 11. Функцын ажиллах зарчим_**' , out.width = "40%"}
knitr::include_graphics(rep("images/11.png", 1))
```

R программын хамгийн энгийн, их хэрэглэгддэг функц нь статистик бодолт болон график гаргах функцууд юм.

-   sum(data), mean(data), median(data), sd(data), var(data), max(data), min(data), quantile(data), summary(data)
-   plot(data)

```{r echo=TRUE}
a <- c(1:5)
b <- 2
result_add <- a + b
result_add
result_sum <- sum(a, b)
result_sum
result_mean <- mean(a, b)
result_mean
```

```{r echo=TRUE}
a <- 1:100
b <- a^2
par(mfrow = c(2, 2))
plot(a, b, type = "l")
plot(b, a, type = "l")
plot(x = a, y = b, type = "l")
plot(y = b, x = a, type = "l")
```

### **R package суулгах, ашиглах**

#### **Package гэж юу вэ?**

Package нь ямарваа эсвэл ижил төсөөтэй сэдвийн хүрээнд хамаарагдах өгөгдлийн багц, тэдгээрийн боломжит функцуудыг агуулсан багц хэрэглүүр юм.
Жишээ нь: статистик, орон зайн шинжилгээ, график.

Хэн ч package-г хөгжүүлж, боловсруулж болох бөгөөд бусдтай хуваалцах, ашиглах бүрэн боломжтойв

Comprehensive R Archive Network (CRAN)-аар дамжуулан олон багцуудыг ашиглах боломжтой ба одоо GitHub болон бусад багцын сангууд дээр илүү олон багцууд байршдаг болсон.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 12. Бүх package-ийн талаарх мэдээлэл_** <https://rdrr.io/>' , out.width = "70%"}
knitr::include_graphics(rep("images/12.png", 1))
```

#### **Package хэрхэн суулгах бэ?**

Консолын мөрөнд дээрээ **install.packages("package_name")** гэж бичээд шууд уншуулж болно.
Мөн RStudio программын цэсээс RStudio\>Tools\>Install packages гэж ороод гарч ирсэн цонх дээр package-ийн нэрээ бичээд суулгаж болно.

```{r echo=FALSE, fig.height=8, fig.asp=0.2, fig.align = 'center', fig.cap='**_Зураг 13. Package суулгах харилцах цонх_** ' , out.width = "70%"}
knitr::include_graphics(rep("images/13.png", 1))
```
